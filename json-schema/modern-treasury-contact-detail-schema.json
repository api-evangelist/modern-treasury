{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/contact_detail",
  "title": "contact_detail",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "object": {
      "type": "string"
    },
    "live_mode": {
      "type": "boolean",
      "description": "This field will be true if this object exists in the live environment or false if it exists in the test environment."
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time"
    },
    "discarded_at": {
      "type": "string",
      "format": "date-time",
      "nullable": true
    },
    "contact_identifier": {
      "type": "string"
    },
    "contact_identifier_type": {
      "type": "string",
      "enum": [
        "email",
        "phone_number",
        "website"
      ]
    }
  },
  "additionalProperties": false,
  "minProperties": 8,
  "required": [
    "id",
    "object",
    "live_mode",
    "created_at",
    "updated_at",
    "discarded_at",
    "contact_identifier",
    "contact_identifier_type"
  ]
}